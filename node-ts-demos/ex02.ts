// number, string, boolean, object, undefined, any, or custom types

// const/let/var

let myName: string;
let myAge: number;

myName = 'Vinod Kumar';
myAge = 48;

let info: any = `My name is ${myName} and I am ${myAge} years old.`;

console.log(info);